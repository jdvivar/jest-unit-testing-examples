// This index provides a safe refactor from the previous versions,
// all importing clients would have their import statements intact

import { prop3Selector } from "./pureSelectors";
import { prop3DerivedSelector } from "./derivedSelectors";

export {
  prop3Selector,
  prop3DerivedSelector
}

